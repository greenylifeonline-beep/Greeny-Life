{
  "mcpServers": {
    "raios": {
      "url": "http://127.0.0.1:8787/mcp",
      "comment": "ChatGPT Apps / Developer Mode uses a remote Streamable HTTP MCP server, not a local stdio server and not custom GPT Actions. Cursor may also use stdio below for local ops."
    }
  }
}
