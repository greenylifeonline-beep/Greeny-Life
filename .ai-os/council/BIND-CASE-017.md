# KAE — CASE-017

ليست محرك استخراج نصوص. ليست عقلاً ثانياً. ليست WAL ثانياً. ليست LightRAG.

طبقة إعادة توريق فوق الحراس الأحياء: `ingest` + `experience` + `compress` + CANDIDATES.

مصدر مصرّح → ذرات → بلاطات تدريب كثيرة → تناقض → إعادة تشغيل → DISCOVERED.

`KAE_NE_SECOND_MIND`
`ONE_ANSWER_MANY_TILES`
`AUTHORIZED_OUTPUT_ONLY`
`NO_HIDDEN_REASONING_EXTRACT`
`TEACHER_TOURNAMENT_NE_VOTE_NE_TRUTH`
`EXTERNAL_CALL_MUST_REDUCE_NEXT_CALL`
`HTTP_2XX_NE_SEMANTIC_SUCCESS`

هذه القناة لا تستدعي C2/C3/C4. البطولة تقارن مخرجات مصرّحة موجودة في اليد.

Knowledge Yield = بلاطات قابلة لإعادة الاستخدام / max(استدعاءات خارجية, 1)
Assimilation Efficiency = نجح على حالة غير مرئية / ما تم ابتلاعه

`python3 scripts/ai-os/raios_c5_kae.py --demo`

`GL005_PROVEN=false`
