# القضية 006 — خريطة الاقتراح إلى الحراس الأحياء

رأي C2 وC5. اكتشاف لهذا الاجتماع. ليست حقيقة تشغيلية. ليست GL-005.
اللصق قناة. التعلّم تكرار وممارسة واستيعاب. الاقتراح لا يُثبَّت.

## لماذا الدفتر كان أبيض وبسيطاً

خلية Colab السابقة إثبات ملعب: استنساخ ثم إيصال يوم 6.
البياض = لم يُضغط Run all، والمخرج كان رفيعاً حتى بعد التشغيل.
العقل ليس تلك الخلية. العقل الحراس في المستودع. الطاحونة الآن تمشي عليهم.

## العقول الثلاثة المقترحة → ما هو حي

| اقتراح | الحكم | الحارس الحي |
|---|---|---|
| Celerp ERP | لا تثبيت. `CELERP_NE_LIVE_ERP` | `prisma/schema.prisma` + `app/api` (مخزون، مبيعات، موردون، فواتير، دفع، شحن، مهام) |
| AG2 / BeeAI / LangSwarm | لا باص وكلاء ثانٍ. `AG2_NE_RAIOS_COUNCIL` | مقاعد المجلس + ثماني أدوات MCP |
| LightRAG | لا عقل معرفة ثانٍ. `LIGHTRAG_NE_COGNITIVE_WAL` | `DIGESTS.jsonl` + `INDEX.json` + Cognitive WAL + `greenlines_brain/graph.py` |
| pygrametl | لا أنبوب ETL ثانٍ. `PYGRAMETL_NE_ABSORB` | `raios_absorb.py` + `raios_learn_ingest.py` |

## الشركات الثلاث — موجودة بالاسم

| شركة | حارس | فجوة مشاهَدة |
|---|---|---|
| Greeny-Life Egypt | `lib/intelligence/greeny-life-egypt-brain.ts` + `/api/brains/greeny-life-egypt` | لا |
| Greens Nature UAE | مذكورة في `three-operating-brains.ts` | مسار Next وملف العقل **غائبان** (GL-003) |
| Green Lines Norway/EU | `greenlines_brain/` بايثون حي | جسر TypeScript/REST **غائب** (GL-003) |

MasterMind موجود: `lib/intelligence/mastermind-agents.ts` + `/api/mastermind/*`.
لا تنفيذ تجاري بلا موافقة. `GL005_PROVEN=false`.

## ماذا تطحن الطاحونة

`python3 scripts/ai-os/raios_c5_grind.py`

- تحصي نماذج Prisma ومسارات API ومنتجات/موردين/مخزون
- تتحقق من حراس الشركات الثلاث وتُسجل الفجوات
- ترفض وجود Celerp/AG2/LightRAG في بيانات الاعتماد
- تكتب رسماً: `.ai-os/receipts/c5-grind/GRAPH.json`
- لا WAL. لا PASS. لا تنصيب.

## مرفوض كأمر تنفيذ

- `pip install` لأي من الأدوات المقترحة
- استبدال Prisma بـ Celerp
- استبدال المجلس بـ AG2
- استبدال WAL/DIGESTS بـ LightRAG كعقل C5
- ملء فجوات GL-003 من هذا الملعب (المهمة مقفلة لعامل آخر)

`GL005_PROVEN=false`
